# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                                       Application.index

# Test methods
GET		/test									Application.test
GET		/test2									Application.test2
GET		/parse									Application.parseXML
GET		/testLogin								Application.testLogin

# methods
GET		/login									Application.login
GET		/load									Application.loadXmlFromSite
POST	/upload									Application.uploadXML

# simulate methods
GET		/wish									Application.getDistributenWishs
POST	/wish									Application.postDistributionWishs
GET		/production								Application.getProductionPlan
POST	/production								Application.postProductionPlan
GET		/order 									Application.getProductionOrders
POST	/order									Application.postProductionOrders
GET		/capacity								Application.getCapacity
POST	/capacity								Application.postCapacity
GET		/disposition							Application.getDisposition
POST	/disposition							Application.postDisposition

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/                                staticDir:public

# Catch all
*       /{controller}/{action}                  {controller}.{action}